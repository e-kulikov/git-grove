# git-grove

Manage repositories as a bare clone surrounded by Git worktrees.

## Development

The project uses the pinned Rust toolchain declared in `mise.toml`. Run the
test suite with `cargo test`.
